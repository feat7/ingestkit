-- IngestKit Generated Schema
-- Schema Version: 1.0
-- DO NOT EDIT MANUALLY - Generated by: ingestkit schema compile

-- Fired when a user shares an article
CREATE TABLE IF NOT EXISTS events_article_shared (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    article_id VARCHAR(255) NOT NULL,
    article_title VARCHAR(255) NOT NULL,
    platform VARCHAR(255) NOT NULL,
    metadata JSONB NULL,
    user_id VARCHAR(255) NULL,
    session_id VARCHAR(255) NOT NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_article_shared_article_id ON events_article_shared(article_id);
CREATE INDEX IF NOT EXISTS idx_article_shared_platform ON events_article_shared(platform);
CREATE INDEX IF NOT EXISTS idx_article_shared_user_id ON events_article_shared(user_id);
CREATE INDEX IF NOT EXISTS idx_article_shared_session_id ON events_article_shared(session_id);
CREATE INDEX IF NOT EXISTS idx_article_shared_timestamp ON events_article_shared(timestamp);


-- Fired when a user posts a comment
CREATE TABLE IF NOT EXISTS events_comment_posted (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    user_id VARCHAR(255) NOT NULL,
    session_id VARCHAR(255) NOT NULL,
    article_id VARCHAR(255) NOT NULL,
    comment_id VARCHAR(255) NOT NULL,
    parent_comment_id VARCHAR(255) NULL,
    comment_length BIGINT NOT NULL,
    metadata JSONB NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_comment_posted_user_id ON events_comment_posted(user_id);
CREATE INDEX IF NOT EXISTS idx_comment_posted_session_id ON events_comment_posted(session_id);
CREATE INDEX IF NOT EXISTS idx_comment_posted_article_id ON events_comment_posted(article_id);
CREATE INDEX IF NOT EXISTS idx_comment_posted_comment_id ON events_comment_posted(comment_id);
CREATE INDEX IF NOT EXISTS idx_comment_posted_parent_comment_id ON events_comment_posted(parent_comment_id);
CREATE INDEX IF NOT EXISTS idx_comment_posted_timestamp ON events_comment_posted(timestamp);


-- Fired when a user subscribes to newsletter
CREATE TABLE IF NOT EXISTS events_newsletter_subscribed (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    email VARCHAR(255) NOT NULL,
    subscription_type VARCHAR(255) NOT NULL,
    source VARCHAR(255) NULL,
    metadata JSONB NULL,
    user_id VARCHAR(255) NULL,
    session_id VARCHAR(255) NOT NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_newsletter_subscribed_email ON events_newsletter_subscribed(email);
CREATE INDEX IF NOT EXISTS idx_newsletter_subscribed_subscription_type ON events_newsletter_subscribed(subscription_type);
CREATE INDEX IF NOT EXISTS idx_newsletter_subscribed_source ON events_newsletter_subscribed(source);
CREATE INDEX IF NOT EXISTS idx_newsletter_subscribed_user_id ON events_newsletter_subscribed(user_id);
CREATE INDEX IF NOT EXISTS idx_newsletter_subscribed_session_id ON events_newsletter_subscribed(session_id);
CREATE INDEX IF NOT EXISTS idx_newsletter_subscribed_timestamp ON events_newsletter_subscribed(timestamp);


-- Fired when a user searches for content
CREATE TABLE IF NOT EXISTS events_search_performed (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    session_id VARCHAR(255) NOT NULL,
    query VARCHAR(255) NOT NULL,
    results_count BIGINT NOT NULL,
    clicked_result_id VARCHAR(255) NULL,
    metadata JSONB NULL,
    user_id VARCHAR(255) NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_search_performed_session_id ON events_search_performed(session_id);
CREATE INDEX IF NOT EXISTS idx_search_performed_query ON events_search_performed(query);
CREATE INDEX IF NOT EXISTS idx_search_performed_clicked_result_id ON events_search_performed(clicked_result_id);
CREATE INDEX IF NOT EXISTS idx_search_performed_user_id ON events_search_performed(user_id);
CREATE INDEX IF NOT EXISTS idx_search_performed_timestamp ON events_search_performed(timestamp);


-- Fired when a user views a product page
CREATE TABLE IF NOT EXISTS events_product_viewed (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    metadata JSONB NULL,
    user_id VARCHAR(255) NOT NULL,
    currency VARCHAR(255) NOT NULL DEFAULT 'USD',
    session_id VARCHAR(255) NOT NULL,
    product_id VARCHAR(255) NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    product_category VARCHAR(255) NOT NULL,
    price DECIMAL(20, 2) NOT NULL,
    source VARCHAR(255) NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_product_viewed_user_id ON events_product_viewed(user_id);
CREATE INDEX IF NOT EXISTS idx_product_viewed_session_id ON events_product_viewed(session_id);
CREATE INDEX IF NOT EXISTS idx_product_viewed_product_id ON events_product_viewed(product_id);
CREATE INDEX IF NOT EXISTS idx_product_viewed_product_category ON events_product_viewed(product_category);
CREATE INDEX IF NOT EXISTS idx_product_viewed_source ON events_product_viewed(source);
CREATE INDEX IF NOT EXISTS idx_product_viewed_timestamp ON events_product_viewed(timestamp);


-- Fired when an order is successfully placed
CREATE TABLE IF NOT EXISTS events_order_completed (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    discount_amount DECIMAL(20, 2) NULL,
    user_id VARCHAR(255) NOT NULL,
    order_id VARCHAR(255) NOT NULL,
    total_amount DECIMAL(20, 2) NOT NULL,
    currency VARCHAR(255) NOT NULL DEFAULT 'USD',
    payment_method VARCHAR(255) NOT NULL,
    shipping_address JSONB NOT NULL,
    discount_code VARCHAR(255) NULL,
    metadata JSONB NULL,
    session_id VARCHAR(255) NOT NULL,
    cart_id VARCHAR(255) NULL,
    num_items BIGINT NOT NULL,
    items JSONB NOT NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_order_completed_user_id ON events_order_completed(user_id);
CREATE INDEX IF NOT EXISTS idx_order_completed_order_id ON events_order_completed(order_id);
CREATE INDEX IF NOT EXISTS idx_order_completed_session_id ON events_order_completed(session_id);
CREATE INDEX IF NOT EXISTS idx_order_completed_cart_id ON events_order_completed(cart_id);
CREATE INDEX IF NOT EXISTS idx_order_completed_timestamp ON events_order_completed(timestamp);


-- Fired when a new user signs up
CREATE TABLE IF NOT EXISTS events_user_signup (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    signup_source VARCHAR(255) NULL,  -- Where the signup originated
    signup_device VARCHAR(255) NULL,  -- Device type used for signup
    referrer VARCHAR(255) NULL,  -- Referrer URL where user came from
    country VARCHAR(255) NULL,  -- User country from signup
    utm_campaign VARCHAR(255) NULL,  -- Marketing campaign identifier
    metadata JSONB NULL,  -- Additional flexible metadata
    user_id VARCHAR(255) NOT NULL,  -- Unique user identifier
    email VARCHAR(255) NOT NULL,  -- User email address
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_user_signup_user_id ON events_user_signup(user_id);
CREATE INDEX IF NOT EXISTS idx_user_signup_timestamp ON events_user_signup(timestamp);


-- Fired when a user makes a purchase
CREATE TABLE IF NOT EXISTS events_purchase (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    order_id VARCHAR(255) NOT NULL,  -- Unique order identifier
    amount DECIMAL(20, 2) NOT NULL,  -- Purchase amount
    currency VARCHAR(255) NULL DEFAULT 'USD',  -- Currency code
    payment_method VARCHAR(255) NULL,  -- Payment method used
    items JSONB NULL,  -- Array of purchased items
    user_id VARCHAR(255) NOT NULL,  -- User who made the purchase
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_purchase_order_id ON events_purchase(order_id);
CREATE INDEX IF NOT EXISTS idx_purchase_user_id ON events_purchase(user_id);
CREATE INDEX IF NOT EXISTS idx_purchase_timestamp ON events_purchase(timestamp);


-- Fired when a user views a page
CREATE TABLE IF NOT EXISTS events_page_view (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    session_id VARCHAR(255) NOT NULL,  -- Session identifier
    page_url VARCHAR(255) NOT NULL,  -- Full page URL
    page_title VARCHAR(255) NULL,  -- Page title
    referrer VARCHAR(255) NULL,  -- Referrer URL
    duration_ms BIGINT NULL,  -- Time spent on page (milliseconds)
    metadata JSONB NULL,  -- Additional page metadata
    user_id VARCHAR(255) NULL,  -- User ID (if logged in)
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_page_view_session_id ON events_page_view(session_id);
CREATE INDEX IF NOT EXISTS idx_page_view_user_id ON events_page_view(user_id);
CREATE INDEX IF NOT EXISTS idx_page_view_timestamp ON events_page_view(timestamp);


-- Fired when a user views a blog article
CREATE TABLE IF NOT EXISTS events_article_viewed (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    metadata JSONB NULL,
    user_id VARCHAR(255) NULL,
    article_title VARCHAR(255) NOT NULL,
    read_time_seconds BIGINT NULL,
    referrer VARCHAR(255) NULL,
    session_id VARCHAR(255) NOT NULL,
    article_id VARCHAR(255) NOT NULL,
    author VARCHAR(255) NOT NULL,
    category VARCHAR(255) NOT NULL,
    tags JSONB NULL,
    source VARCHAR(255) NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_article_viewed_user_id ON events_article_viewed(user_id);
CREATE INDEX IF NOT EXISTS idx_article_viewed_session_id ON events_article_viewed(session_id);
CREATE INDEX IF NOT EXISTS idx_article_viewed_article_id ON events_article_viewed(article_id);
CREATE INDEX IF NOT EXISTS idx_article_viewed_author ON events_article_viewed(author);
CREATE INDEX IF NOT EXISTS idx_article_viewed_category ON events_article_viewed(category);
CREATE INDEX IF NOT EXISTS idx_article_viewed_source ON events_article_viewed(source);
CREATE INDEX IF NOT EXISTS idx_article_viewed_timestamp ON events_article_viewed(timestamp);


-- Fired when a user adds an item to cart
CREATE TABLE IF NOT EXISTS events_added_to_cart (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    user_id VARCHAR(255) NOT NULL,
    session_id VARCHAR(255) NOT NULL,
    quantity BIGINT NOT NULL,
    price DECIMAL(20, 2) NOT NULL,
    currency VARCHAR(255) NOT NULL DEFAULT 'USD',
    product_id VARCHAR(255) NOT NULL,
    product_name VARCHAR(255) NOT NULL,
    cart_total DECIMAL(20, 2) NULL,
    metadata JSONB NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_added_to_cart_user_id ON events_added_to_cart(user_id);
CREATE INDEX IF NOT EXISTS idx_added_to_cart_session_id ON events_added_to_cart(session_id);
CREATE INDEX IF NOT EXISTS idx_added_to_cart_product_id ON events_added_to_cart(product_id);
CREATE INDEX IF NOT EXISTS idx_added_to_cart_timestamp ON events_added_to_cart(timestamp);


-- Fired when a user begins checkout process
CREATE TABLE IF NOT EXISTS events_checkout_started (
    tenant_id VARCHAR(255) NOT NULL,
    event_id BIGSERIAL,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    metadata JSONB NULL,
    user_id VARCHAR(255) NOT NULL,
    session_id VARCHAR(255) NOT NULL,
    cart_id VARCHAR(255) NOT NULL,
    num_items BIGINT NOT NULL,
    cart_total DECIMAL(20, 2) NOT NULL,
    currency VARCHAR(255) NOT NULL DEFAULT 'USD',
    items JSONB NOT NULL,
    PRIMARY KEY (tenant_id, event_id)
) PARTITION BY LIST (tenant_id);

CREATE INDEX IF NOT EXISTS idx_checkout_started_user_id ON events_checkout_started(user_id);
CREATE INDEX IF NOT EXISTS idx_checkout_started_session_id ON events_checkout_started(session_id);
CREATE INDEX IF NOT EXISTS idx_checkout_started_cart_id ON events_checkout_started(cart_id);
CREATE INDEX IF NOT EXISTS idx_checkout_started_timestamp ON events_checkout_started(timestamp);


