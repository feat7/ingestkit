// Code generated by IngestKit Schema Compiler. DO NOT EDIT.

package consumer

import (
	"context"
	"encoding/json"
	"fmt"
	"log"

	"github.com/feat7/ingestkit/generated/models"
	"github.com/feat7/ingestkit/generated/storage"
	"github.com/feat7/ingestkit/internal/messaging"
)

// BatchHandler processes event batches and writes them to storage
type BatchHandler struct {
	writer *storage.Writer
}

// NewBatchHandler creates a new batch handler with the given storage writer
func NewBatchHandler(writer *storage.Writer) *BatchHandler {
	return &BatchHandler{writer: writer}
}

// ProcessBatch unmarshals, groups, and writes events by type
func (h *BatchHandler) ProcessBatch(ctx context.Context, envelopes []*messaging.EventEnvelope) error {
	// Track schema versions in batch for observability
	schemaVersions := make(map[string]int)
	for _, envelope := range envelopes {
		schemaVersions[envelope.SchemaVersion]++
	}
	log.Printf("📦 Processing batch: %d events (schema versions: %v)", len(envelopes), schemaVersions)

	// Group events by type for batch insertion
	addedToCarts := make([]*models.AddedToCart, 0)
	articleShareds := make([]*models.ArticleShared, 0)
	articleVieweds := make([]*models.ArticleViewed, 0)
	checkoutStarteds := make([]*models.CheckoutStarted, 0)
	commentPosteds := make([]*models.CommentPosted, 0)
	newsletterSubscribeds := make([]*models.NewsletterSubscribed, 0)
	orderCompleteds := make([]*models.OrderCompleted, 0)
	pageViews := make([]*models.PageView, 0)
	productVieweds := make([]*models.ProductViewed, 0)
	purchases := make([]*models.Purchase, 0)
	searchPerformeds := make([]*models.SearchPerformed, 0)
	userSignups := make([]*models.UserSignup, 0)

	// Unmarshal and group events
	for _, envelope := range envelopes {
		switch envelope.EventType {
		case "added_to_cart":
			var event models.AddedToCart
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal added_to_cart: %w", err)
			}
			addedToCarts = append(addedToCarts, &event)

		case "article_shared":
			var event models.ArticleShared
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal article_shared: %w", err)
			}
			articleShareds = append(articleShareds, &event)

		case "article_viewed":
			var event models.ArticleViewed
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal article_viewed: %w", err)
			}
			articleVieweds = append(articleVieweds, &event)

		case "checkout_started":
			var event models.CheckoutStarted
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal checkout_started: %w", err)
			}
			checkoutStarteds = append(checkoutStarteds, &event)

		case "comment_posted":
			var event models.CommentPosted
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal comment_posted: %w", err)
			}
			commentPosteds = append(commentPosteds, &event)

		case "newsletter_subscribed":
			var event models.NewsletterSubscribed
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal newsletter_subscribed: %w", err)
			}
			newsletterSubscribeds = append(newsletterSubscribeds, &event)

		case "order_completed":
			var event models.OrderCompleted
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal order_completed: %w", err)
			}
			orderCompleteds = append(orderCompleteds, &event)

		case "page_view":
			var event models.PageView
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal page_view: %w", err)
			}
			pageViews = append(pageViews, &event)

		case "product_viewed":
			var event models.ProductViewed
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal product_viewed: %w", err)
			}
			productVieweds = append(productVieweds, &event)

		case "purchase":
			var event models.Purchase
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal purchase: %w", err)
			}
			purchases = append(purchases, &event)

		case "search_performed":
			var event models.SearchPerformed
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal search_performed: %w", err)
			}
			searchPerformeds = append(searchPerformeds, &event)

		case "user_signup":
			var event models.UserSignup
			if err := unmarshalEvent(envelope, &event); err != nil {
				return fmt.Errorf("failed to unmarshal user_signup: %w", err)
			}
			userSignups = append(userSignups, &event)

		default:
			return fmt.Errorf("unknown event type: %s", envelope.EventType)
		}
	}

	// Write batches to database
	if len(addedToCarts) > 0 {
		if err := h.writer.WriteAddedToCartBatch(addedToCarts); err != nil {
			return fmt.Errorf("failed to write added_to_cart batch: %w", err)
		}
		log.Printf("✅ Wrote %d added_to_cart events", len(addedToCarts))
	}

	if len(articleShareds) > 0 {
		if err := h.writer.WriteArticleSharedBatch(articleShareds); err != nil {
			return fmt.Errorf("failed to write article_shared batch: %w", err)
		}
		log.Printf("✅ Wrote %d article_shared events", len(articleShareds))
	}

	if len(articleVieweds) > 0 {
		if err := h.writer.WriteArticleViewedBatch(articleVieweds); err != nil {
			return fmt.Errorf("failed to write article_viewed batch: %w", err)
		}
		log.Printf("✅ Wrote %d article_viewed events", len(articleVieweds))
	}

	if len(checkoutStarteds) > 0 {
		if err := h.writer.WriteCheckoutStartedBatch(checkoutStarteds); err != nil {
			return fmt.Errorf("failed to write checkout_started batch: %w", err)
		}
		log.Printf("✅ Wrote %d checkout_started events", len(checkoutStarteds))
	}

	if len(commentPosteds) > 0 {
		if err := h.writer.WriteCommentPostedBatch(commentPosteds); err != nil {
			return fmt.Errorf("failed to write comment_posted batch: %w", err)
		}
		log.Printf("✅ Wrote %d comment_posted events", len(commentPosteds))
	}

	if len(newsletterSubscribeds) > 0 {
		if err := h.writer.WriteNewsletterSubscribedBatch(newsletterSubscribeds); err != nil {
			return fmt.Errorf("failed to write newsletter_subscribed batch: %w", err)
		}
		log.Printf("✅ Wrote %d newsletter_subscribed events", len(newsletterSubscribeds))
	}

	if len(orderCompleteds) > 0 {
		if err := h.writer.WriteOrderCompletedBatch(orderCompleteds); err != nil {
			return fmt.Errorf("failed to write order_completed batch: %w", err)
		}
		log.Printf("✅ Wrote %d order_completed events", len(orderCompleteds))
	}

	if len(pageViews) > 0 {
		if err := h.writer.WritePageViewBatch(pageViews); err != nil {
			return fmt.Errorf("failed to write page_view batch: %w", err)
		}
		log.Printf("✅ Wrote %d page_view events", len(pageViews))
	}

	if len(productVieweds) > 0 {
		if err := h.writer.WriteProductViewedBatch(productVieweds); err != nil {
			return fmt.Errorf("failed to write product_viewed batch: %w", err)
		}
		log.Printf("✅ Wrote %d product_viewed events", len(productVieweds))
	}

	if len(purchases) > 0 {
		if err := h.writer.WritePurchaseBatch(purchases); err != nil {
			return fmt.Errorf("failed to write purchase batch: %w", err)
		}
		log.Printf("✅ Wrote %d purchase events", len(purchases))
	}

	if len(searchPerformeds) > 0 {
		if err := h.writer.WriteSearchPerformedBatch(searchPerformeds); err != nil {
			return fmt.Errorf("failed to write search_performed batch: %w", err)
		}
		log.Printf("✅ Wrote %d search_performed events", len(searchPerformeds))
	}

	if len(userSignups) > 0 {
		if err := h.writer.WriteUserSignupBatch(userSignups); err != nil {
			return fmt.Errorf("failed to write user_signup batch: %w", err)
		}
		log.Printf("✅ Wrote %d user_signup events", len(userSignups))
	}

	return nil
}

// unmarshalEvent unmarshals the event envelope payload into a typed event struct
func unmarshalEvent(envelope *messaging.EventEnvelope, event interface{}) error {
	// Unmarshal raw JSON directly into the typed event struct
	if err := json.Unmarshal(envelope.Payload, event); err != nil {
		return fmt.Errorf("failed to unmarshal event: %w", err)
	}

	// Set tenant_id and timestamp from envelope
	switch e := event.(type) {
	case *models.AddedToCart:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.ArticleShared:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.ArticleViewed:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.CheckoutStarted:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.CommentPosted:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.NewsletterSubscribed:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.OrderCompleted:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.PageView:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.ProductViewed:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.Purchase:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.SearchPerformed:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	case *models.UserSignup:
		e.TenantID = envelope.TenantID
		e.Timestamp = envelope.Timestamp
	}

	return nil
}
